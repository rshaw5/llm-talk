{
  "name": "llm-talk",
  "private": true,
  "scripts": {
    "setup": "find . -maxdepth 1 ! -name 'dist' ! -name '.' ! -name '.git' | xargs rm -r",
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.43.0-beta.1",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest"
  }
}
